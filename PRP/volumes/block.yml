# Block Storage
# =============
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filebench-block
spec:
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 50Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
